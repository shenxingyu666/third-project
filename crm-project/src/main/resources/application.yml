# 数据源信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/huige?serverTimezone=UTC
    username: root
    password: rootroot

mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 9000