# 数据源信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.11.243:3306/db_thirdproject?serverTimezone=GMT%2B8&charsetEncoding=utf8
    username: root
    password: root
  jackson:
    date-format: yyyy-MM-dd

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 9000

logging:
  level:
    com:
      shangma :
        cn:
          mapper: debug
  file:
    name: E:\\user.log
